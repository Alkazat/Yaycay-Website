{
  "ci": {
    "collect": {
      "startServerCommand": "npm run start -- -p 3100",
      "url": ["http://localhost:3100/"],
      "numberOfRuns": 3,
      "settings": { "preset": "desktop" }
    },
    "assert": {
      "assertions": {
        "_comment_performance": "0.90 floor: the site is genuinely fast (FCP ~0.4s, TBT 0, CLS <0.01); the LCP on a cold first visit is the privacy-gated client consent banner, which an SSG site can't server-gate without forcing dynamic rendering everywhere. 0.90 still hard-fails real regressions.",
        "categories:performance": ["error", { "minScore": 0.9 }],
        "categories:seo": ["error", { "minScore": 0.95 }],
        "categories:accessibility": ["error", { "minScore": 0.95 }],
        "categories:best-practices": ["warn", { "minScore": 0.95 }]
      }
    },
    "upload": { "target": "temporary-public-storage" }
  }
}
